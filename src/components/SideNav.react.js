var React = require('react');

var SideNav = React.createClass({
    render: function () {
        return (
            <div>
                <h1>Inside SideNav</h1>
            </div>
        );
    }
});

module.exports = SideNav;
